using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace VPC.WebApi.Utility
{
    internal static class ApiConstant
    {
        internal static readonly string CustomErrorMessage = "Something went wrong, please try again later!";
        //internal static readonly string ResourceNotFoundErrorMessage = "Resource not found";

        ////Donot change this
        //internal static readonly int DefaultPageSize = 100;

        //internal static readonly string BadRequestErrorMessage = "Bad request";
        //internal static readonly string IdRequiredErrorMessage = "The id field is required";
        //internal static readonly string UnAuthorisedErrorMessage = "You don't have permission.";

        //internal static readonly string InUseRollMessage = "Re-map the User's Role Identifier and retry";

        //internal static readonly string InUserSubscriberMessage = "Subscriber already exist";

        //internal static string GetDuplicationErrorMessage(string controllerPrefix, string messageSuffix = "")
        //{
        //    if (messageSuffix != "")
        //    {
        //        return string.Format("{0},  with same {1} already exists!", controllerPrefix, messageSuffix);
        //    }
        //    return string.Format("{0}  with same name already exists!", controllerPrefix);
        //}
        //internal static readonly string SuccessfullyRegisteredMessage = "Successfully registered";
        //internal static readonly string AlreadyRegisteredMessage = "Already registered";
        //internal static readonly string SuccessfullyCreateddMessage = "Successfully created";
        //internal static readonly string SuccessfullyDeletedMessage = "Successfully deleted";
        //internal static readonly string SuccessfullyUpdatedMessage = "Successfully updated";
        //internal static readonly string SuccessfullyActionTakenMessage = "Successfully";
        //internal static readonly string InvalidUser = "Invalid user";
    }
}
